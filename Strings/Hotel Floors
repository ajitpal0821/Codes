#include <bits/stdc++.h> 
int hotelBookings(vector<string> queries) {
    // Write your codes here.
    int c=0;
    int n=queries.size();
    for(int i=0;i<n;i++){
        if(queries[i][0]=='+')
        c++;
        
    }
    return c;

}
