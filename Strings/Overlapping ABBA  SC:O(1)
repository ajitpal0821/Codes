#include<stack>
bool findOverlap(string &s){
int c=0;
int n=s.size();
    for (int i = 0; i < n-1; i+=2) {
            if(s[i]=='B' && s[i+1]=='A' || s[i]=='A' && s[i+1]=='B')
                c++;
            
        

    }
    return c>=2;
}
