#include <bits/stdc++.h> 
string convertString(string str) 
{
	// WRITE YOUR CODE HERE
	int n=str.size();
	for(int i=0;i<n;i++){
		if(i==0)
		str[i]=toupper(str[i]);
		else if( str[i]==' '){
			if(i<n){
				str[i+1]=toupper(str[i+1]);
			}
		}
	}
	return str;
}
